# ============================================
# SportsHub – API & WebSocket (Railway)
# ============================================

# Django API on Railway
EXPO_PUBLIC_API_BASE=https://web-production-8d095.up.railway.app

# WebSocket (Node server.js on Railway). Replace the host below with your
# actual Node service URL after you add that service in Railway (same repo,
# root dir, REDIS_URL set). Example: wss://stato-ws-production-xxxx.up.railway.app
EXPO_PUBLIC_WS_URL=wss://your-ws-service.up.railway.app

# After changing .env, restart Expo: npx expo start -c
